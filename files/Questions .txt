18 May 2026

Zerto data =========

- how are we uploading scripts 

- why did we name the first sheet zerto data. Zerto data is very generic. In this sheets its taking site name, VPG labels >> good 

- where are VPG labels assigned to VPG in gui? 

Hypervisor data ==========


- wording for the table has to simplify, confusing atm. 

Hypervisor_Data_Protected_ZVM_VMs: All VMs that need to be included in VPGs, per Protected ZVM Site. >>>> can we get rid of the long desc to short one -

Protected VMs: All VMs that need to be included in VPGs 
Protected Volumes: All volumes that are attached to the protected VMs 
Protected NICs 

- why do we need to be explicit about volumes? When you supply the VM it will pull >>>> need it in cases where they may use pre-seeded volume, if so, then why is it that it needs a VM specified, it does not need a VM correct for a presided volume? What happens when we don't give any volume? 

- why do we need CPU count and memory? Even in GUI we dont ask it correct? 

- in my view in this sheet the VM is driving the data hence the VM should be in the first column. 

- go cell by cell in plain English how validation will look like 

- what will happen if certain cell is left black? For example, recovery data store, if the cell is empty the xl sheet is still allowing it to be blank, which means the the program is checking it. 

- all data needs to be centered aligned - some cells are left and some cells are right 

- I will need to the password to put wrong data. 

- again on the hypervisor data sheet - recovery host - the host details should be the first column 

- so if the customers want to change the name for example of a datastore name, 



04 June 2026 

** zerto data ** 

Boot order 
- Boot order meta group name: 3 tier web app 
- boot order group name: ex - web server 
- so you are defining, in the table, a combination of meta group + group + the delay time 

- what is the purpose of the 'group id' is it a combination id? 



** hypervisor data ** 

- why are VM on the protected site listed here in this sheet? Zerto does not need to know what VMs are there on the site it only needs to know which VMs are to be protected? 

- All values in Default_VPG_Settings are mandatory. The only exceptions are the (Scratch) Journal Size hard Limit and Warning Threshold values, which must be empty if the respective Unit is set to Unlimited. >>> does it mean journal does not have unlimited and scratch journal can have it? >>>> got it both!

- 



17 June - Wed - Welcome day - 5:30 to 7:00 

- frist preloved session 17th june - becs we may not be intrested park walled 
- ⁠sports kit testing 17th June 

24th June - Wed - Transition day - all day 

- Pre-Loved Uniform Shop from 3.30pm - 4.30pm - transition day - 24th June 
- ⁠⁠submit transition day RSVP >>>>>>> Done 

10th July - Fri - Welcome Picnic - 3:45 to 5:30 pm 
 
- Selling hoodies 
- RSVP >>>>>>>>> Done 

TTD 

- Get a notepad and pens 
- music lessions 
- Summer reading challenge 


Q
- where to buy brand new school uniform 
- transition day agreed point who is going to give us the point 
- what is ICT inductions on the first day of school on 3rd sep 


======================================================================

Rename the JSON output to an honest handoff name like vca_check_dump.json.

Keep validation_status at the top so the API program can stop if it is not passed.

Resolve workbook defaults before dumping so blank Excel fields become effective values.

Shape the JSON around future creation objects instead of raw workbook tabs.

Keep literal names, but always include enough context around them.

Add identifier placeholders like "id": null for fields the API program must resolve.

Separate reference data from objects-to-create data.

Add a clear creation order for the API team.

Add source traceability with sheet, table, and table row ID.

Add a name-resolution policy that says not found and multiple matches must fail.

Create a JSON contract document explaining every field.

Create one golden passing workbook and JSON dump as the API team’s test fixture.

Add a contract version at the top of the JSON.

Keep raw extracted workbook data only as debug/reference, not as the main API handoff section.






























